To Run the project

1. install node.js if it is not installed already

2. copy the link of the repo and paste it in the command line in a new terminal

3. run "Git Clone"

4. run "npm install" to install all the dependencies of the project

5. Run the project in the command line using "npm run dev"
